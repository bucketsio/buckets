<h1>Users</h1>

<a href="#add" class="btn btn-default">Add a user</a>

{{#if items}}
  <ul class="nav nav-stacked nav-pills">
  {{#each items}}
    <li><a href="#"><strong>{{gravatar email_hash}} {{name}}</strong> <span class="muted">{{email}}</span></a></li>
  {{/each}}
  </ul>
{{/if}}